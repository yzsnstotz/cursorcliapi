[project]
name = "codex-api-gateway"
version = "0.1.0"
description = "Expose `codex exec` as a small OpenAI-compatible HTTP API."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115.0",
  "pydantic>=2.7.0",
  "uvicorn[standard]>=0.30.0",
]

